namespace SliceCloud.Repository.Enums;

/// <summary>
/// Represents user's roles for consistency across the entire project.
/// </summary>
public enum UserRoles : int
{
    Admin = 1,
    Manager = 2,
    Chef = 3
}
